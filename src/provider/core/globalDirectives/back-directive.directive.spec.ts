import { BackDirectiveDirective } from './back-directive.directive';

describe('BackDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BackDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
